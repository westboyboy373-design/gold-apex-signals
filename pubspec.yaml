name: gold_apex_signals
description: Gold & Forex signal platform with coin wallet and manual WhatsApp payment verification.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  url_launcher: ^6.3.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
